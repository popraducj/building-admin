@using Microsoft.Extensions.Localization
@inject IStringLocalizer<string> Localizer


@{
    ViewData["Title"] = Localizer["The page was not found"];
    ViewData["BackgroundImageAlt"] = Localizer["BackgroundImageAlt"];
}

    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1 class="title">@Localizer["The page was not found"]</h1>
                <h4>@Localizer["The page you are trying to reach was not found"]</h4> <br />
                <a href="@Url.AbsoluteAction("Index", "Home")" class="btn btn-primary btn-raised btn-lg">@Localizer["Home"]</a>
            </div>
        </div>
    </div>
</div>